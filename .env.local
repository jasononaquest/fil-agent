# ===========================================
# Local Development Configuration
# ===========================================
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=REDACTED_API_KEY

# Local MCP server (SSE transport)
MCP_SERVER_URL=http://localhost:8000/sse

# Rails Event Push (disabled for local - uses ActionCable directly)
# RAILS_EVENTS_URL=http://localhost:3000/api/internal/agent_events
# INTERNAL_API_TOKEN=test_internal_token_12345

# OpenTelemetry (optional for local)
GOOGLE_CLOUD_AGENT_ENGINE_ENABLE_TELEMETRY=false
